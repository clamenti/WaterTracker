import React from 'react';
import ReactDOM from 'react-dom/client';
import WaterTrackerApp from './WaterTrackerApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WaterTrackerApp />
  </React.StrictMode>
);
